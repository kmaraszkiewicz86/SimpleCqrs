namespace SimpleCqrs
{
    /// <summary>
    /// Represents a result type that indicates a void or no-value result.
    /// </summary>
    /// <remarks>This class can be used in scenarios where a method or operation does not return a value, but
    /// still needs to convey success or completion. It serves as a placeholder for methods that conceptually return a
    /// result but do not produce any meaningful data.</remarks>
    public class VoidResult { }
}